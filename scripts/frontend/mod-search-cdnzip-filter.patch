--- src/api/modApi.jsx	2026-09-21 09:33:55
+++ src/api/modApi.jsx	2026-09-21 09:33:55
@@ -1,8 +1,8 @@
 import { http } from "../utils/http";
 
-async function searchMod(lang, cluster,text, page, size) {
+async function searchMod(lang, cluster, text, page, size, excludeCdnZip) {
 
-    const url = `/api/mod/search?text=${text}&page=${page}&size=${size}&lang=${lang}`
+    const url = `/api/mod/search?text=${text}&page=${page}&size=${size}&lang=${lang}&excludeCdnZip=${excludeCdnZip}`
     const response = await http.get(url,{
         headers: {
             'Cluster': cluster,
--- src/pages/Mod/Workshop/index.jsx	2026-09-21 09:33:55
+++ src/pages/Mod/Workshop/index.jsx	2026-09-21 09:53:36
@@ -1,6 +1,6 @@
 /* eslint-disable react/prop-types */
 import {useEffect, useState} from 'react';
-import {Row, Col, Card, Input, Pagination, Button, message, Empty, Spin} from 'antd';
+import {Row, Col, Card, Input, Pagination, Button, message, Empty, Spin, Checkbox} from 'antd';
 import {StarFilled} from '@ant-design/icons';
 import {useParams} from "react-router-dom";
 import {useTranslation} from "react-i18next";
@@ -68,6 +68,20 @@
                 style={{marginBottom: 8}}
             />
 
+            {modinfo.cdnZip && (
+                <div style={{
+                    fontSize: '12px',
+                    color: '#cf1322',
+                    background: '#fff1f0',
+                    border: '1px solid #ffa39e',
+                    borderRadius: 4,
+                    padding: '4px 8px',
+                    marginBottom: 8,
+                }}>
+                    ⚠️ CDN-zip 旧版封装，本环境无法自动加载，不建议添加
+                </div>
+            )}
+
             <div style={{
                 fontSize: '12px',
                 color: '#999',
@@ -144,6 +158,8 @@
     const [total, setTotal] = useState(0)
     const [text, setText] = useState("")
     const [searching, setSearching] = useState(false)
+    // CDN-zip 过滤默认开启：box64 环境下该类型模组无法被游戏自动下载认证
+    const [onlySupported, setOnlySupported] = useState(true)
 
     const [messageApi, contextHolder] = message.useMessage();
     const {cluster} = useParams()
@@ -185,9 +201,9 @@
         })
     }
 
-    const updateModList = (text, page, pageSize) => {
+    const updateModList = (text, page, pageSize, exclude = onlySupported) => {
         setSearching(true)
-        searchMod(lang, cluster, text, page, pageSize).then(data => {
+        searchMod(lang, cluster, text, page, pageSize, exclude).then(data => {
             setModList(data.data.data)
             setPage(data.data.page)
             setPageSize(data.data.size)
@@ -217,18 +233,28 @@
         <>
             {contextHolder}
 
-            <Search
-                placeholder={t('mod.search.placeholder')}
-                allowClear
-                enterButton
-                // size="large"
-                loading={searching}
-                onSearch={onSearch}
-                style={{
-                    // width: '300px',
-                    marginBottom: '24px',
-                }}
-            />
+            <div style={{display: 'flex', alignItems: 'center', gap: '12px', marginBottom: '24px'}}>
+                <Search
+                    placeholder={t('mod.search.placeholder')}
+                    allowClear
+                    enterButton
+                    // size="large"
+                    loading={searching}
+                    onSearch={onSearch}
+                    style={{flex: 1}}
+                />
+                <Checkbox
+                    checked={onlySupported}
+                    onChange={(e) => {
+                        setOnlySupported(e.target.checked)
+                        if (text) {
+                            updateModList(text, 1, pageSize, e.target.checked)
+                        }
+                    }}
+                >
+                    仅显示可自动加载的模组
+                </Checkbox>
+            </div>
 
             <Spin spinning={searching}>
                 {modList.length === 0 && !searching ? (
